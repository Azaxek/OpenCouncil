# Core API
fastapi>=0.110.0
uvicorn[standard]>=0.27.0
httpx>=0.27.0
beautifulsoup4>=4.12.0
pydantic>=2.5.0
python-multipart>=0.0.9
python-dotenv>=1.0.0

# Groq AI client
openai>=1.0.0

# Encryption for API keys
cryptography>=42.0.0

# Database — PostgreSQL for Vercel, SQLite for local
psycopg2-binary>=2.9.0

# Vercel serverless adapter
mangum>=0.17.0

# Minimal image handling (Pillow only, not PyTorch)
Pillow>=10.0.0
numpy>=1.24.0